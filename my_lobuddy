# Lobuddy 项目技术亮点（简历用）

## 项目定位
基于 nanobot AI Agent 框架二次开发的桌面宠物 AI 助手应用，将底层 agent 能力产品化为可视化、可交互、可成长的桌面陪伴应用。

---

## 核心技术栈

- **GUI**: PySide6 (Qt for Python) - 无边框窗口、系统托盘、全局热键
- **后端**: Python 3.11+, asyncio, threading
- **数据层**: SQLite + Repository Pattern
- **AI**: nanobot (AI Agent 框架) + OpenAI API
- **配置**: Pydantic + python-dotenv
- **测试**: pytest + pytest-cov

---

## 相比 nanobot 的核心进步

### 1. 产品形态升级：从 CLI/框架到桌面应用

**nanobot 提供的**: 轻量级 AI Agent 框架，包含：
- Agent 核心循环 (agent/loop.py)
- 工具调用系统 (文件、搜索、shell、web、MCP)
- 记忆系统 (history.jsonl + MEMORY/SOUL/USER.md)
- 多渠道接入 (CLI / API / Telegram / Discord / 飞书等)
- 配置驱动 (JSON 配置文件)

**Lobuddy 增加的**:
- PySide6 桌面宠物 UI (无边框、透明背景、GIF 动画)
- 系统托盘常驻应用
- 全局热键唤出
- 设置对话框界面
- 任务聊天面板 (Markdown 安全渲染、图片上传)

**简历表述**:
> 将 nanobot CLI/框架形态封装为 PySide6 桌面宠物产品，实现无边框透明窗体、系统托盘常驻、全局热键唤出等原生桌面应用体验。

---

### 2. 业务数据持久化：从文件记忆到结构化存储

**nanobot 的存储**: 文件型记忆系统
- history.jsonl - 对话历史
- MEMORY.md / SOUL.md / USER.md - 记忆文档
- 基于文本的持久化

**Lobuddy 增加的**:
- SQLite 本地数据库 (Repository Pattern)
- 多表结构设计：
  - `pet_state`: 宠物状态 (level/exp/evolution_stage/mood/personality)
  - `task_record`: 任务主记录
  - `task_result`: 执行结果
  - `chat_session/message`: 聊天会话历史
  - `app_settings`: 应用配置
  - `unlocked_abilities`: 已解锁能力
- 敏感配置本地加密存储 (基于用户指纹的轻量 XOR 加密)
- 原子事务保证数据一致性

**简历表述**:
> 设计 SQLite Repository 持久化层，实现宠物状态、任务流水、聊天记录、应用设置的统一管理，支持 JSON 列存储复杂人格数据，敏感配置本地加密。

---

### 3. 游戏化成长系统：从无状态对话到状态机驱动

**nanobot**: 无状态对话，每次请求独立

**Lobuddy 增加的**:
- EXP/等级系统 (1-10 级，分段 EXP 曲线)
- 进化阶段 (3 阶段：幼体/成长期/完全体)
- 性格系统 (五维性格模型)
- 能力解锁机制
- 心情/状态变化
- 任务难度分级与奖励差异化

**简历表述**:
> 设计带 EXP/等级(1-10级分段曲线)/进化阶段(3阶段状态机)/五维性格属性的宠物领域模型，实现任务完成→EXP奖励→升级→进化的完整成长闭环，通过 Pydantic 模型 + 状态机约束业务状态流转。

---

### 4. 任务生命周期管理：从单次调用到编排系统

**nanobot**: 单次 agent run，工具调用执行即结束

**Lobuddy 增加的**:
- 任务队列与异步执行 (submit → queue → execute)
- Qt Signal/Slot 机制实现 UI 与任务解耦
- 任务状态机 (QUEUED → RUNNING → COMPLETED/FAILED)
- 原子性结果落库
- 超时控制与取消机制
- 任务完成后自动触发成长反馈

**简历表述**:
> 实现任务从提交、排队、执行、结果落库到宠物成长反馈的完整生命周期编排，通过 Qt Signal 与线程锁实现 UI 与任务执行的异步解耦。

---

### 5. AI 能力扩展与安全增强

**nanobot 提供**: 基础工具调用、MCP、子 agent

**Lobuddy 增加的**:
- 动态配置生成 (运行时创建独立配置，避免配置污染，限制文件权限类Unix系统)
- 多模态图片分析 (SubagentFactory 独立子进程运行，避免阻塞 UI)
- 历史压缩 (Token 使用量控制)
- 工具注册与隔离机制 (ToolRegistry 运行时临时注册与清理)
- 安全 guardrails:
  - 参数类型检查
  - 危险命令扫描
  - 路径/URL 白名单校验
  - API Key 脱敏
- Token 使用量统计与会话级阈值控制（自动历史压缩）

**简历表述**:
> 在 nanobot 之上封装任务执行适配层，增加动态配置、多模态图片分析、历史压缩、token 统计与安全 guardrails，实现带超时控制、异常脱敏和临时资源清理的可恢复 AI 任务执行链路。

---

### 6. 应用级稳定性与用户体验

**nanobot**: 作为框架，退出即结束

**Lobuddy 增加的**:
- 启动引导流程 (目录创建、健康检查、配置初始化)
- 可靠退出机制 (信号通知 → 资源清理 → 强制关闭)
- 热键线程管理 (回收避免泄漏)
- 应用生命周期管理 (启动引导、健康检查、配置持久化)
- 健康检查 CLI 工具
- 双层配置系统 (.env 默认值 + SQLite 运行时覆盖)
- 事件总线解耦 (EventBus 串联 UI、任务、成长系统)

**简历表述**:
> 构建完整的应用生命周期管理，包括启动引导、健康检查、双层配置系统、托盘退出联动清理、热键线程回收、事件总线解耦，实现可长期驻留的桌面级稳定性。

---

## 简历项目描述（可直接复制）

### 版本 1 - 简洁版
**Lobuddy | PySide6 桌面宠物 AI 助手**
基于 nanobot AI Agent 框架二次开发，将底层 agent 能力产品化为可视化桌面宠物应用。使用 PySide6 实现无边框透明窗体与系统托盘常驻，SQLite Repository 模式实现宠物状态/任务/聊天记录持久化，设计 EXP/等级/进化游戏化成长系统，封装多模态图片分析与安全 guardrails，实现从单次对话到可持续运行的长期状态管理。

### 版本 2 - 详细版
**Lobuddy | 桌面宠物 AI 助手 | Python/PySide6/SQLite**
- 产品化：将 nanobot AI Agent 框架从 CLI 形态封装为 PySide6 桌面宠物产品，实现无边框透明窗体、GIF 动画、拖拽交互、系统托盘常驻、全局热键唤出等原生桌面体验
- 数据层：设计 SQLite Repository 持久化层，采用 Repository Pattern + 原子事务，统一管理宠物状态、任务流水、聊天记录、应用配置，敏感信息本地轻量加密存储
- 业务层：实现 EXP/等级/进化阶段/性格五维模型的游戏化成长系统，任务完成自动触发 EXP 奖励、升级判定、能力解锁闭环
- 任务编排：构建任务生命周期管理系统，Qt Signal 实现 UI 与异步任务解耦，支持超时控制、取消机制、原子结果落库
- AI 扩展：封装 nanobot 适配层，增加动态配置(带文件权限限制类Unix)、多模态图片分析子 Agent(独立进程隔离)、工具运行时注册、历史压缩、Token 统计、危险命令 guardrails
- 稳定性：实现应用启动引导、健康检查、双层配置、可靠退出清理、热键线程回收等桌面级生命周期管理

---

## 技术亮点关键词

- PySide6 / Qt / FramelessWindow / System Tray
- SQLite / Repository Pattern / Atomic Transaction / Encryption
- Pydantic / Domain Model / State Machine
- asyncio / threading / Qt Signal-Slot / Task Queue
- AI Agent / Tool Calling / Multi-modal / Token Management
- Guardrails / Security / Error Handling / EventBus

---

生成时间: 2026-04-23
